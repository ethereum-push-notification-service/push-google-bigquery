import { ENV } from '../constants';
import { IUser } from '../types';
export declare type ChatUpdateUserOptionsType = {
    user: string;
    profilePictureCID?: string;
    name?: string;
    env?: ENV;
};
export declare const updateUser: (options: ChatUpdateUserOptionsType) => Promise<IUser>;
