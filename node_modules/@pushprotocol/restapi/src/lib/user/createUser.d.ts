import Constants, { ENV } from '../constants';
import { SignerType, ProgressHookType, IUser } from '../types';
export declare type CreateUserProps = {
    env?: ENV;
    account?: string;
    signer?: SignerType;
    version?: typeof Constants.ENC_TYPE_V1 | typeof Constants.ENC_TYPE_V3;
    progressHook?: (progress: ProgressHookType) => void;
};
export declare const create: (options: CreateUserProps) => Promise<IUser>;
