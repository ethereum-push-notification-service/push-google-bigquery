import { IUser } from '../types';
import { ENV } from '../constants';
export declare type getNFTProfile = {
    env?: ENV;
    did: string;
};
export declare const getNFTProfile: (options: getNFTProfile) => Promise<IUser>;
