import { ENV } from '../constants';
import { SignerType, IUser, ProgressHookType } from '../types';
export declare type UpgradeUserProps = {
    env?: ENV;
    account?: string;
    signer: SignerType;
    progressHook?: (progress: ProgressHookType) => void;
};
export declare const upgrade: (options: UpgradeUserProps) => Promise<IUser>;
